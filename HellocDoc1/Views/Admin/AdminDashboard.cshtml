@{
    ViewData["Title"] = "Dashboard";
    Layout = "~/Views/Shared/_AdminDashboardLayout.cshtml";
}

@model Services.Models.AdminDashboardViewModel

<div class="d-flex justify-content-center">
    <div class="" style="width:95%">

        <div class="row  mt-3 mb-4 mx-auto">

            <div class="col-lg-2 col-4 px-2 pb-2 status" id="1">
                <div class="px-2 py-1 new newActive rounded-3 pt-1 text-md-left text-center" id="pills-new-tab" onclick="newState()">

                    <i class="bi bi-patch-plus icons"></i>

                    <span class="ms-1 fw-bold newTxt">NEW</span>

                    <span class="mt-2 h4 newTxt d-block">@Model.newCount</span>
                </div>
            </div>
            <div class="col-lg-2 col-4 px-2 pb-2 status" id="2">
                <div class="px-2 py-1 rounded-3 pending pt-1 text-md-left text-center" id="pills-pending-tab" onclick="pendingState()">

                    <i class="bi bi-clock-history icons"></i>

                    <span class="ms-1 fw-bold">PENDING</span>

                    <span class="mt-2 h4 d-block">@Model.pendingCount</span>
                </div>
            </div>
            <div class="col-lg-2 col-4 px-2 pb-2 status" id="3">
                <div class="px-2 py-1 rounded-3 active pt-1 text-md-left text-center" id="pills-active-tab" onclick="activeState()">

                    <i class="bi bi-check2-circle icons"></i>

                    <span class="ms-1 fw-bold">ACTIVE</span>

                    <span class="mt-2 h4 d-block">@Model.activeCount</span>
                </div>
            </div>
            <div class="col-lg-2 col-4 px-2 status" id="4">
                <div class="px-2 py-1 rounded-3 conclude pt-1 text-md-left text-center" id="pills-conclude-tab" onclick="concludeState()">

                    <i class="bi bi-arrow-counterclockwise icons"></i>

                    <span class="ms-1 fw-bold">CONCLUDE</span>

                    <span class="mt-2 h4 d-block">@Model.concludeCount</span>
                </div>
            </div>
            <div class="col-lg-2 col-4 px-2 status" id="5">
                <div class="px-2 py-1 rounded-3 toclose pt-1 text-md-left text-center" id="pills-close-tab" onclick="closeState()">

                    <i class="bi bi-x-circle icons"></i>

                    <span class="ms-1 fw-bold">TO CLOSE</span>

                    <span class="mt-2 h4 d-block">@Model.tocloseCount</span>
                </div>
            </div>
            <div class="col-lg-2 col-4 px-2 status" id="6">
                <div class="px-2 py-1 rounded-3 unpaid pt-1 text-md-left text-center" id="pills-unpaid-tab" onclick="unpaidState()">
                    <span class=" fw-bold mt-2 icons">$</span>
                    <span class="ms-1 fw-bold">UNPAID</span>

                    <span class="mt-2 h4 d-block">@Model.unpaidCount</span>
                </div>
            </div>
        </div>
        <div class="d-flex flex-row justify-content-between mt-5 mb-3">
            <div>
                <span class="h4">Patient </span>
                <span class=" fw-bold" id="status" style="color:#203f9a;"></span>
            </div>
            <div class="d-flex gap-1">
                <a onclick="sendmail()" class="bg-info text-white rounded-3 p-2" style="cursor:pointer;">
                    <i class="bi bi-reply h5 mr-1"></i>
                    <span class="link-text">Send Link</span>
                </a>
                <a asp-action="CreateRequest" class="bg-info text-white rounded-3 p-2 text-decoration-none" style="cursor:pointer;">
                    <i class="bi bi-pencil-square  mr-1"></i>
                    <span class="link-text">Create Requests</span>
                </a>
                <a class="bg-info text-white rounded-3 p-2" id="exportbtn" style="cursor:pointer;">
                    <i class="bi bi-reply-all  mr-1"></i>
                    <span class="link-text">Export</span>
                </a>
                <a class="bg-info text-white rounded-3 p-2" style="cursor:pointer;">
                    <i class="bi bi-reply-all  mr-1"></i>
                    <span class="link-text">Export All</span>
                </a>
                <a class="bg-info text-white rounded-3 p-2" style="cursor:pointer;">
                    <i class="bi bi-person-square  mr-1"></i>
                    <span class="link-text">Request DTY Support</span>
                </a>
            </div>
        </div>
        <div class="shadow rounded-3 pt-4">

            <div class="px-2 row" style="">
                <div class="form col-xl-3 col-md-6">
                    <input type="search" class="form-control form-input " id="searchpatient" onkeyup="searchpatient()" placeholder=" Search Patients">
                </div>
                <div class="form col-xl-2 col-md-6 mt-3 m-md-0">
                    <select class="form-select select-region" id="region">
                        <option selected>All Regions</option>
                        @foreach (var item in Model.regions)
                        {
                            <option value="@item.RegionId">@item.Name</option>
                        }
                    </select>
                </div>


                <div class="col-xl-7 col-md-12 mt-2 ms-1 m-xl-0 text-xl-end">
                    <button class="text-secondary mr-2 border-info rounded-2 bg-transparent selected">

                        <span>ALL</span>
                    </button>
                    <button class="text-secondary mr-2 border-0 bg-transparent selected" id="1">
                        <i class="bi bi-circle-fill text-success mr-1"></i>
                        <span>Patient</span>
                    </button>
                    <button class="text-secondary mr-2 border-0 bg-transparent selected" id="2">
                        <i class="bi bi-circle-fill text-warning mr-1"></i>
                        <span>Family/Friend</span>
                    </button>
                    <button class="text-secondary mr-2 border-0 bg-transparent selected" id="3">
                        <i class="bi bi-circle-fill mr-1" style="color:pink;"></i>
                        <span>Business</span>
                    </button>
                    <button class="text-secondary  mr-2 border-0 bg-transparent selected" id="4">
                        <i class="bi bi-circle-fill text-primary mr-1"></i>
                        <span>Concierge</span>
                    </button>
                    <button class="text-secondar border-0 bg-transparent">
                        <i class="bi bi-circle-fill mr-1" style="color:purple;"></i>
                        <span class="mr-2">VIP</span>
                    </button>
                </div>


            </div>

            <div id="tableContent" class="table-responsive mt-4 mb-5">
            </div>
        </div>
    </div>
</div>
<div id="mail"></div>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
<script>
    // console.log("hello");

    // $(document).ready(function () {

    //     console.log("hello2");
    //     var url;
    //     $('.status').on('click', function (e) {
    //         console.log("two");
    //         e.preventDefault();
    //         var target = $(this).attr('id');
    //         console.log(target);
    //         var status;

    //         switch (target) {
    //             case '1':
    //                 status = '1';
    //                 url = '/Admin/NewState'
    //                 break;
    //             case '2':
    //                 status = '2';
    //                 url = '/Admin/PendingState'
    //                 break;
    //             case '3':
    //                 status = '3';
    //                 url = '/Admin/ActiveState'
    //                 break;
    //             case '4':
    //                 status = '4';
    //                 url = '/Admin/ConcludeState'
    //                 break;
    //             case '5':
    //                 status = '5';
    //                 url = '/Admin/ToCLoseState'
    //                 break;
    //             case '6':
    //                 status = '6';
    //                 url = '/Admin/UnpaidState'
    //                 break;
    //             default:
    //         }
    //         console.log(url);

    //     });

    //     $('.selected').on('click', function (e) {
    //         console.log("two");
    //         e.preventDefault();
    //         var target = $(this).attr('id');
    //         console.log(target);
    //         var requesttype;
    //         switch (target) {
    //             case '1':
    //                 requesttype = '1';
    //                 break;
    //             case '2':
    //                 requesttype = '2';
    //                 break;
    //             case '3':
    //                 requesttype = '3';
    //                 break;
    //             case '4':
    //                 requesttype = '4';
    //                 break;
    //             default:
    //         }
    //         $.ajax({
    //             url: url,
    //             data: { "status": status, "requesttype": requesttype },
    //             success: function (response) {
    //                 $('#tableContent').html(response);
    //             },
    //             error: function (xhr, status, error) {
    //                 console.error(error);
    //             }
    //         });

    //     });
    // });

    var filterdata = {
        status: 1,
        url: "",
        patientname: "",
        requesttype: 5,
        requestedPage: 1,
        region: 0,
    }

    function AjaxAll() {
        $.ajax({
            url: filterdata.url,
            type: "POST",
            data: filterdata,
            dataType: "html",
            success: function (data) {
                let isLogin = data.includes("Login");
                if (isLogin) {
                    window.location.reload();
                }
                else {
                    $("#tableContent").html(data);
                    $("#status").html("(New)");
                }

            },
            error: function (error) {
                console.log(error)
                $("#tableContent").html('An error has occurred');
            }
        });
    }

    $(".selected").click(function () {
        var requestType = $(this).attr('id');

        switch (requestType) {
            case '1':
                requestType = 1;
                break;
            case '2':
                requestType = 2;
                break;
            case '3':
                requestType = 3;
                break;
            case '4':
                requestType = 4;
                break;
            default:
        }
        filterdata.requesttype = requestType;
        filterdata.requestedPage = 1;
        AjaxAll();
    })

    function changepage(CurrentPage) {
        console.log("pagination");
        filterdata.requestedPage = CurrentPage;
        AjaxAll();
    }

    function searchpatient() {
        filterdata.patientname = $("#searchpatient")[0].value;
        filterdata.requestedPage = 1;
        AjaxAll();
    }

    $('#region').on('change', function () {
        filterdata.region = $(this).val();
        AjaxAll();

    })

    $(document).ready(function () {
        filterdata.status = 1;
        $.ajax({
            url: "/Admin/NewState",
            type: "POST",
            data: filterdata,
            dataType: "html",
            success: function (data) {
                let isLogin = data.includes("Login");
                if (isLogin) {
                    window.location.reload();
                }
                else {
                    $("#tableContent").html(data);
                    $("#status").html("(New)");
                    filterdata.url = "/Admin/NewState";

                }
            },
            error: function (error) {
                console.log(error)
                alert("No Projects Found");
                $("#tableContent").html('An error has occurred');
            }
        });
        $('#pills-new-tab').click(function () {
            filterdata.status = 1;
            $.ajax({
                url: "/Admin/NewState",
                type: "POST",
                data: filterdata,
                dataType: "html",
                success: function (data) {
                    let isLogin = data.includes("Login");
                    if (isLogin) {
                        window.location.reload();
                    }
                    $("#tableContent").html(data);
                    $("#status").html("(New)");
                    filterdata.url = "/Admin/NewState";
                },
                error: function () {
                    alert("No Projects Found");
                    $("#tableContent").html('An error has occurred');
                }
            });
        });
        $('#pills-pending-tab').click(function () {
            filterdata.status = 2;
            $.ajax({
                url: "/Admin/PendingState",
                type: "POST",
                data: filterdata,
                dataType: "html",
                success: function (data) {
                    let isLogin = data.includes("Login");
                    if (isLogin) {
                        window.location.reload();
                    }
                    else {
                        $("#tableContent").html(data);
                        $("#status").html("(Pending)");
                        filterdata.url = "/Admin/PendingState";

                    }

                },
                error: function () {
                    alert("No Projects Found");
                    $("#tableContent").html('An error has occurred');
                }
            });
        });
        $('#pills-active-tab').click(function () {
            filterdata.status = 3;
            $.ajax({
                url: "/Admin/ActiveState",
                type: "POST",
                data: filterdata,
                dataType: "html",
                success: function (data) {
                    let isLogin = data.includes("Login");
                    if (isLogin) {
                        window.location.reload();
                    }
                    else {
                        $("#tableContent").html(data);
                        $("#status").html("(Active)");
                        filterdata.url = "/Admin/ActiveState";

                    }

                },
                error: function () {
                    alert("No Projects Found");
                    $("#tableContent").html('An error has occurred');
                }
            });
        });
        $('#pills-conclude-tab').click(function () {
            filterdata.status = 4;
            $.ajax({
                url: "/Admin/ConcludeState",
                type: "POST",
                data: filterdata,
                dataType: "html",
                success: function (data) {
                    let isLogin = data.includes("Login");
                    if (isLogin) {
                        window.location.reload();
                    }
                    else {
                        $("#tableContent").html(data);
                        $("#status").html("(Conclude)");
                        filterdata.url = "/Admin/ConcludeState";

                    }

                },
                error: function () {
                    alert("No Projects Found");
                    $("#tableContent").html('An error has occurred');
                }
            });
        });
        $('#pills-close-tab').click(function () {
            filterdata.status = 5;
            $.ajax({
                url: "/Admin/ToCloseState",
                type: "POST",
                data: filterdata,
                dataType: "html",
                success: function (data) {
                    let isLogin = data.includes("Login");
                    if (isLogin) {
                        window.location.reload();
                    }
                    else {
                        $("#tableContent").html(data);
                        $("#status").html("(Close)");
                        filterdata.url = "/Admin/ToCloseState";

                    }
                },
                error: function () {
                    alert("No Projects Found");
                    $("#tableContent").html('An error has occurred');
                }
            });
        });
        $('#pills-unpaid-tab').click(function () {
            filterdata.status = 6;
            $.ajax({
                url: "/Admin/UnpaidState",
                type: "POST",
                data: filterdata,
                dataType: "html",
                success: function (data) {
                    let isLogin = data.includes("Login");
                    if (isLogin) {
                        window.location.reload();
                    }
                    else {
                        $("#tableContent").html(data);
                        $("#status").html("(Unpaid)");
                        filterdata.url = "/Admin/UnpaidState";
                    }
                },
                error: function () {
                    alert("No Projects Found");
                    $("#tableContent").html('An error has occurred');
                }
            });
        });
    });

    function sendmail() {
        $.ajax({
            url: "/Admin/SendMailDetails",
            type: "POST",
            data: {},
            dataType: "html",
            success: function (data) {
                $("#mail").html(data);
                const myModal = new bootstrap.Modal("#staticBackdrop", {});
                myModal.show();
            },
            error: function () {
                alert("No Projects Found");
            }
        });
    };

    $('#exportbtn').click(function () {
        if (typeof (filterdata.patientname == "undefined")) {
            filterdata.patientname = ""
        }
        var link = document.createElement('a');
        link.href = '/Admin/ExportData?requesttype=' + filterdata.requesttype + "&patientname=" + filterdata.patientname + "&status=" + filterdata.status
        link.style.display = 'none';
        document.body.appendChild(link);
        link.click();
        document.body.removeChild(link);
    });

    function newState() {
        document.getElementById("pills-new-tab").classList.add("newActive");
        document.getElementById("pills-pending-tab").classList.remove("pendingActive");
        document.getElementById("pills-active-tab").classList.remove("activeStateActive");
        document.getElementById("pills-conclude-tab").classList.remove("concludeActive");
        document.getElementById("pills-close-tab").classList.remove("tocloseActive");
        document.getElementById("pills-unpaid-tab").classList.remove("unpaidActive");
    }

    function pendingState() {
        document.getElementById("pills-new-tab").classList.remove("newActive");
        document.getElementById("pills-pending-tab").classList.add("pendingActive");
        document.getElementById("pills-active-tab").classList.remove("activeStateActive");
        document.getElementById("pills-conclude-tab").classList.remove("concludeActive");
        document.getElementById("pills-close-tab").classList.remove("tocloseActive");
        document.getElementById("pills-unpaid-tab").classList.remove("unpaidActive");
    }
    function activeState() {
        document.getElementById("pills-new-tab").classList.remove("newActive");
        document.getElementById("pills-pending-tab").classList.remove("pendingActive");
        document.getElementById("pills-active-tab").classList.add("activeStateActive");
        document.getElementById("pills-conclude-tab").classList.remove("concludeActive");
        document.getElementById("pills-close-tab").classList.remove("tocloseActive");
        document.getElementById("pills-unpaid-tab").classList.remove("unpaidActive");
    }
    function concludeState() {
        document.getElementById("pills-new-tab").classList.remove("newActive");
        document.getElementById("pills-pending-tab").classList.remove("pendingActive");
        document.getElementById("pills-active-tab").classList.remove("activeStateActive");
        document.getElementById("pills-conclude-tab").classList.add("concludeActive");
        document.getElementById("pills-close-tab").classList.remove("tocloseActive");
        document.getElementById("pills-unpaid-tab").classList.remove("unpaidActive");
    }
    function closeState() {
        document.getElementById("pills-new-tab").classList.remove("newActive");
        document.getElementById("pills-pending-tab").classList.remove("pendingActive");
        document.getElementById("pills-active-tab").classList.remove("activeStateActive");
        document.getElementById("pills-conclude-tab").classList.remove("concludeActive");
        document.getElementById("pills-close-tab").classList.add("tocloseActive");
        document.getElementById("pills-unpaid-tab").classList.remove("unpaidActive");
    }
    function unpaidState() {
        document.getElementById("pills-new-tab").classList.remove("newActive");
        document.getElementById("pills-pending-tab").classList.remove("pendingActive");
        document.getElementById("pills-active-tab").classList.remove("activeStateActive");
        document.getElementById("pills-conclude-tab").classList.remove("concludeActive");
        document.getElementById("pills-close-tab").classList.remove("tocloseActive");
        document.getElementById("pills-unpaid-tab").classList.add("unpaidActive");
    }
</script>
@if (TempData["Success"] != null)
{
    <link rel="stylesheet" href="//cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/css/toastr.min.css" />
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>
    <script type="text/javascript">
        toastr.success('@TempData["Success"]');
    </script>
}