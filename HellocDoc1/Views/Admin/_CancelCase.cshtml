@model Services.Models.CancelCaseViewModel

@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
<div class="modal fade" id="staticBackdrop" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <form id="ConfirmCancel" method="post">
                <div class="modal-header bg-info text-white">
                    <h5 class="modal-title" id="staticBackdropLabel">Confirm Cancellation</h5>
                    <button type="button" class="btn-close bg-black" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="d-flex flex-row">

                        <p class="text-secondary mr-1">Patient Name :</p><p class="text-info fw-bold" id="Name"></p>
                    </div>
                    <div class=" gap-2">
                        <div class="form-floating mb-3">
@*                             @foreach (var item in Model.ReasonForCancel)
                            { *@
                                <select class="form-select text-secondary" id="floatingSelect" aria-label="Floating label select example">
                                    <option value="1">Name</option>
                                </select>
                                <label for="floatingSelect">Reason for Cancellation</label>
                            @* } *@
                        </div>
                        <div class="form-floating mb-3">
                            <textarea class="form-control" asp-for="AdditionalNotes" placeholder="additional notes"
                                      id="floatingTextarea2" style="height: 100px"></textarea>
                            <label for="floatingTextarea2" class="">
                                Provide Additonal Notes
                            </label>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="submit" class="btn bg-info text-white">Confirm</button>
                    <button type="button" class="btn border border-info text-info" data-bs-dismiss="modal">Cancel</button>
                </div>
            </form>
        </div>
    </div>
</div>